/**
 * EST extension library registry.
 *
 * Future sensors such as cameras can add a card here. A device extension card
 * can provide extensionId, name, description, iconURL and the packaged
 * toolbox/blocks/translations/generator script URLs expected by OpenBlock's
 * upload-mode extension loader.
 */
export default [];
